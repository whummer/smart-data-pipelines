{
  "id": "eureka",
  "apiVersion": "v1beta1",
  "kind": "ReplicationController",
  "desiredState": {
    "replicas": 1,
    "replicaSelector": {
      "name": "eureka"
    },
    "podTemplate": {
      "desiredState": {
        "manifest": {
          "version": "v1beta1",
          "id": "eureka",
          "containers": [
            {
              "name": "eureka",
              "image": "${DOCKER_REGISTRY}/riots/service-eureka",
              "ports": [
                {
                  "containerPort": 10000
                }
              ]
            }
          ]
        }
      },
      "labels": {
        "name": "eureka",
        "env": "${RIOTS_ENV}"
      }
    }
  },
  "labels": {
    "name": "eureka",
    "env": "${RIOTS_ENV}"
  }
}