{
    "id": "riots-catalog-${IMAGE_TAG}",
    "apiVersion": "v1beta1",
    "kind": "ReplicationController",
    "desiredState": {
        "replicas": 1,
        "replicaSelector": {
            "name": "riots-catalog"
        },
        "podTemplate": {
            "desiredState": {
                "manifest": {
                    "version": "v1beta1",
                    "id": "riots-catalog",
                    "containers": [
                        {
                            "name": "riots-catalog",
                            "image": "${DOCKER_REGISTRY}/riots/service-catalog:${IMAGE_TAG}",
                            "ports": [
                                {
                                    "name": "riots-catalog-port",
                                    "containerPort": 8083
                                }
                            ],
                            "env" : [
                            	{
                            		"name": "JAVA_OPTS",
                            		"value": "-Deureka.hostname=${EUREKA_HOSTNAME} -Delasticsearch.hostname=${ELASTICSEARCH_HOSTNAME}"
                            	}

                            ]
                        }
                    ]
                }
            },
            "labels": {
                "name": "riots-catalog",
        		"env": "${RIOTS_ENV}"
            }
        }
    },
    "labels": {
        "name": "riots-catalog",
        "env": "${RIOTS_ENV}"
    }
}