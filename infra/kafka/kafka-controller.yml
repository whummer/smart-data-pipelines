kind: ReplicationController
apiVersion: v1
metadata:
  name: kafka
  labels:
    name: kafka
spec:
  replicas: 1
  selector:
    name: kafka
  template:
    metadata:
      labels:
        name: kafka
    spec:
      containers:
          - name: kafka
            image: riox/kafka
            env:
              - name: KAFKA_ZOOKEEPER_CONNECT
                value: zookeeper.dev.svc.cluster.local:2181
            ports:
              - containerPort: 9092
